import React from "react";
import { Box } from "@mui/material";

const Profile = () => {
  return (
    <Box sx={{ maxWidth: '90vw', mx: "auto", p: 2 }}>
      <div>Bem-vindo à página do Usuário!</div>
    </Box>
  );
};

export default Profile;
